1. Jelaskan mengapa penulisan parameter di praktikum 4 ditulis dengan String...
   namaPengunjung!
   ANSWER: varArgs functioned as special parameter where they can accept zero,
   one, or more than one input data, so its like an upgraded array

2. Modifikasi method daftarPengunjung menggunakan for-each loop.
   ANSWER: done

3. Bisakah menggunakan dua tipe data varaargs dalam satu fungsi?
   Jelaskan jawaban Anda berdasarkan aturan varargs di Java, dan
   berikan contohnya!
   ANSWER: tidak bisa, satu varArgs hanyab bisa untuk satu datatype

4. Jelaskan apa yang terjadi jika fungsi daftarPengunjung dipanggil
   tanpa argumen. Apakah program akan error saat kompilasi, error saat
   dijalankan, atau tetap berjalan? Jika tetap berjalan, bagaimana output
   yang dihasilkan?
   ANSWER: tidak error karena varArgs menerima value null/0
