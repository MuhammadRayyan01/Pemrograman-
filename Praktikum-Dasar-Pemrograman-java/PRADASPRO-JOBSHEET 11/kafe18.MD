1. Apakah fungsi tanpa parameter selalu harus bertipe void?
   Dasar Pemrograman 2025
   -ANSWER:no, parameter acted to get the data out of the function, in void we also can use parameter to display the other function

2. Apakah daftar menu pada program kafe dapat ditampilkan tanpa menggunakan fungsi
   Menu()? Modifikasi kode program tersebut untuk dapat menampilkan daftar menu
   tanpa menggunakan fungsi!
   -ANSWER: tidak bisa, karena fungsi perlu dipanggil agar mengeluarkan outputnya, jika dimodifikasi maka sama saja seperti mengerjakan program seperti sebelumnya

3. Jelaskan keuntungan menggunakan fungsi Menu() dibandingkan menulis semua
   perintah penampilan menu langsung di dalam fungsi main.
   -ANSWER: Dengna menggunakan fungsi, penulisan program menjadi semakin rapi dan terstruktur sehingga lebih mudah lagi untuk dibaca

4. Uraikan secara singkat alur eksekusi program ketika fungsi Menu() dipanggil dari main
   (mulai dari program dijalankan sampai daftar menu tampil di layar)
   -ANSWER:in function, actually we are separating a function from the main method, so when we want to call some operation, we just call the variable. We can make the function before or after the main method, so we make the function, and in the main method, we type the varibale name which was menu()
